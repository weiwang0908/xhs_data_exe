# 红薯雷达 运行与打包依赖
# 适配 Python 3.11 / 3.12（3.12 推荐）

# 浏览器兜底采集（静默 / 可见浏览器）
playwright>=1.44.0

# 商品主图缩放与展示
pillow>=10.0.0

# Windows 上保证 Asia/Shanghai 时区可用
tzdata>=2024.1

# 打包工具（仅打包时需要）
pyinstaller>=6.6.0
